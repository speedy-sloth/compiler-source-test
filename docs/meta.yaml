source_name: reserver            # registry key + url prefix
product: paas
area: feature-x
display_name: Reserver Module
version: 1.3.2            # actual version → URL segments + ${reserver:version}
display_version: "1.3"  # optional — human-facing label
compatible_with: "6.3"
status: supported
root_page: overview/index.md
default_kind: how-to
owners:
  - team-x

# ── Variables ──
vars:                             # Source-scoped — only available within this source's pages
  sync_mode: scheduled
  default_timeout: "30"
