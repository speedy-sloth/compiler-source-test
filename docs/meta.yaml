source_name: reserver            # registry key + url prefix
product: paas
display_name: Reserver Module
version: 2.2.3            # actual version → URL segments + ${reserver:version}
display_version: "2.2 LTS"  # optional — human-facing label
compatible_with: "6.4"
status: supported
module_type: extension
root_page: index.md
default_kind: how-to
owners:
  - team-x

# ── Variables ──
vars:                             # Source-scoped — only available within this source's pages
  sync_mode: scheduled
  default_timeout: "30"
